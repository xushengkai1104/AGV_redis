可以将Redis集群是redis提供的分布式数据库方案。
集群通过分片（sharding）来进行数据共享。提供复制和故障转移功能。

可以像ros的节点一样来理解。一个Redis集群通常由多个节点组成。
Node节点之间相互独立。
把几个独立的节点连接起来就是一个集群了。

用port端口号来区分各个节点。


关于集群的标准解释（计算机组成与设计）
集群：通过标准的网络开关将I/O链接起来的计算机集合，以此来构建消息传递机制的多处理器。
集群在通信方面的性能很弱，但是低成本高可用性和快速可扩展性是的它对于Internet服务提供者有很大吸引力。
